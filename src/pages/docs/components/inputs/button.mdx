import { DocsMDXLayout } from "components/docs/DocsMDXLayout.tsx";
import { DocsComponent } from "components/docs/DocsComponent";
import { PropTypes } from "react-view";
import { Button, InputGroup } from "@parssa/universal-ui";
import * as Icon from "react-icons/hi";

export const meta = {
  title: "Button",
  description: "Used to trigger actions and events. Y'know, like a button."
};

<DocsComponent.Primary
  componentName="Button"
  defaultProps={[
    "children",
    "theme",
    "size",
    "variant",
    "disabled",
    {
      name: "leadingIcon",
      type: PropTypes.ReactNode,
      description: "Icon shown before the content.",
      value: "",
      placeholder: '<svg viewBox="0 0 15 15" fill="none" xmlns="http://www.w3.org/2000/svg">'
    },
    {
      name: "trailingIcon",
      type: PropTypes.ReactNode,
      description: "Icon shown after the content.",
      value: "",
      placeholder: '<svg viewBox="0 0 15 15" fill="none" xmlns="http://www.w3.org/2000/svg">'
    },
    {
      name: "icon",
      type: PropTypes.ReactNode,
      description:
        "Renders a uniformly shaped button with a centered icon. Children is used as alt-text",
      value: "",
      placeholder: '<svg viewBox="0 0 15 15" fill="none" xmlns="http://www.w3.org/2000/svg">'
    }
  ]}
  scope={{
    Button
  }}
  imports={{
    "@parssa/universal-ui": {
      named: ["Button"]
    }
  }}
/>

## Examples

### With Icons

<DocsComponent.Example
  initialCode={`() => 
      <>
        <Button
          leadingIcon={<Icon.HiBell className="w-full h-full" />}>
          Notifications
        </Button>
        <Button
          trailingIcon={<Icon.HiArrowRight className="w-full h-full" />}>
          Next
        </Button>
      </>
    `}
  scope={{
    Button,
    Icon
  }}
  imports={{
    "@parssa/universal-ui": {
      named: ["Button"]
    },
    "react-icons/hi": {
      default: "Icon"
    }
  }}
/>

You can use the `leadingIcon` and `trailingIcon` props to add icons before/after the button content.

These props accept any ReactNode, so you can use any icon library you want.
If the icons are not being sized correctly, make sure there is no implicit `width/height` props on the icon, and try adding `w-full h-full`.

### Pure Icon

<DocsComponent.Example
  initialCode={`() => 
      <>
        <Button
          icon={<Icon.HiBell className="w-full h-full" />}>
          Notifications
        </Button>
        <Button
          icon={<Icon.HiAdjustments className="w-full h-full" />}>
          Next
        </Button>
      </>
    `}
  scope={{
    Button,
    Icon
  }}
  imports={{
    "@parssa/universal-ui": {
      named: ["Button"]
    },
    "react-icons/hi": {
      default: "Icon"
    }
  }}
/>

For a button with only an icon, you can use the `icon` prop. This will render a button with a uniform size and shape, and the icon will be centered.

The `children` prop is used as the `alt` text for the icon, and will not be visible.

### With InputGroup

<DocsComponent.Example
  initialCode={`() => 
      <>
        <InputGroup>
          <Button
            icon={<Icon.HiBell className="w-full h-full" />}>
            Notifications
          </Button>
          <Button
            icon={<Icon.HiAdjustments className="w-full h-full" />}>
            Next
          </Button>
          <Button>
            Notifications
          </Button>
          <Button
            trailingIcon={<Icon.HiArrowRight className="w-full h-full" />}>
            Next
          </Button>
        </InputGroup>
      </>
    `}
  scope={{
    Button,
    Icon,
    InputGroup
  }}
  imports={{
    "@parssa/universal-ui": {
      named: ["Button", "InputGroup"]
    },
    "react-icons/hi": {
      default: "Icon"
    }
  }}
/>

You can use the `Button` component inside an `InputGroup` to create a segmented button. The `InputGroup` can be used with `Input`, `Select` and `Button`.

Learn more about `InputGroup` [here ->](/docs/components/inputs/input-group).

export default ({ children }) => <DocsMDXLayout meta={meta}>{children}</DocsMDXLayout>;
