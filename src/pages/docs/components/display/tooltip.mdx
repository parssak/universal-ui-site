import { DocsMDXLayout } from "components/docs/DocsMDXLayout.tsx";
import { Card, Text, Button, Tooltip } from "@parssa/universal-ui";
import { Todo } from "components/global/ui/Todo";

export const meta = {
  title: "Tooltip",
  description: "Floating tooltips for displaying additional information."
};

<Todo>This page is still under construction. Please check back later.</Todo>

<Card data-uui="false" className="mt-size-4y bg-theme-pure/25 backdrop-blur-lg grid-pattern">
  <Card.Content className="py-size-2y md:py-size-4y my-size-4y px-size-2x md:px-size-4x grid place-items-center">
    <Tooltip.Root>
      <Tooltip.Trigger>
        <Button>Hover me</Button>
      </Tooltip.Trigger>
      <Tooltip.Content size="sm">
        <Text>Here's a cookie 🍪</Text>
      </Tooltip.Content>
    </Tooltip.Root>
  </Card.Content>
</Card>

export default ({ children }) => <DocsMDXLayout meta={meta}>{children}</DocsMDXLayout>;
