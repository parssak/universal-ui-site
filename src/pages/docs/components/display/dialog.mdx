import { useState } from "react";
import { PropTypes } from "react-view";
import { DocsMDXLayout } from "components/docs/DocsMDXLayout";
import { Button, Input, Dialog, Text } from "@parssa/universal-ui";
import { Todo } from "components/global/ui/Todo";
import { DocsComponent } from "components/docs/DocsComponent";
import { CodeBlock } from "components/global/ui/CodeBlock";
import { RadixDisclaimer } from "components/global/ui/RadixDisclaimer";

export const meta = {
  title: "Dialog",
  description: "Dialog that displays on top the page."
};

<RadixDisclaimer
  componentName="Dialog"
  radixComponentName="Dialog"
  radixURL="https://www.radix-ui.com/docs/primitives/components/dialog"
/>

<DocsComponent.Primary
  defaultProps={[
    {
      name: "children",
      type: PropTypes.ReactNode,
      description: "The content of the Dialog",
      value: "",
      noEdit: true,
    },
    {
      name: "open",
      type: PropTypes.Boolean,
      description: "Icon shown before the content.",
      value: "",
      noEdit: true,
      defaultValue: false,
      placeholder: '<svg viewBox="0 0 15 15" fill="none" xmlns="http://www.w3.org/2000/svg">'
    },
    {
      name: "onOpenChange",
      type: PropTypes.Function,
      description: "Icon shown before the content.",
      value: "",
      noEdit: true,
      placeholder: '<svg viewBox="0 0 15 15" fill="none" xmlns="http://www.w3.org/2000/svg">'
    }
  ]}
  initialCode={`() => {
  const [open, setOpen] = useState(false);

  return (
    <>
      <Button onClick={() => setOpen(true)}>Edit Profile</Button>

      <Dialog open={open} onOpenChange={setOpen}>
        <Dialog.Content>
          <Dialog.Title>Edit Profile</Dialog.Title>
          <Dialog.Description className="mt-size-2y">
            Make changes to your profile here.
          </Dialog.Description>
          <label className="grid grid-cols-4 items-center gap-size-x mt-size-4y ">
            <Text size="sm" as="span" className="font-medium  text-right">
              Name
            </Text>
            <Input className="col-span-3 w-full" />
          </label>
          <label className="grid grid-cols-4 items-center gap-size-x mt-size-2y ">
            <Text size="sm" as="span" className="font-medium  text-right">
              Username
            </Text>
            <Input className="col-span-3 w-full" />
          </label>
          <div className="flex justify-end mt-6">
            <Button theme="brand">Save changes</Button>
          </div>
        </Dialog.Content>
      </Dialog>
    </>
  );
};
`}
scope={{
    Input,
    Button,
    Dialog,
    Text,
    useState
  }}
imports={{
    "@parssa/universal-ui": {
      named: ["Input", "Button", "Dialog", "Text"]
    },
    react: {
      named: ["useState"]
    }
  }}
/>

## Anatomy

<CodeBlock className="mt-size-2y">
  {`<Dialog open={open} onOpenChange={onOpenChange}>
  <Dialog.Content>
    <Dialog.Title>{/* ... */}</Dialog.Title>
    <Dialog.Description>{/* ... */}</Dialog.Description>
  </Dialog.Content>
  {/* (Full-width sections can go here) */}
</Dialog>`}
</CodeBlock>

export default ({ children }) => <DocsMDXLayout meta={meta}>{children}</DocsMDXLayout>;
