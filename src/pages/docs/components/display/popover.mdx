import React from "react";
import { DocsMDXLayout } from "components/docs/DocsMDXLayout.tsx";
import { DocsComponent } from "components/docs/DocsComponent";
import { PropTypes } from "react-view";
import { Card, Text, Input, Button, Popover } from "@parssa/universal-ui";
import { Todo } from "components/global/ui/Todo";
import { CodeBlock } from "components/global/ui/CodeBlock";
import { RadixDisclaimer } from "components/global/ui/RadixDisclaimer";

export const meta = {
  title: "Popover",
  description: "Floating Popovers for displaying additional information."
};

<DocsComponent.Primary
  defaultProps={[
    {
      name: "children",
      type: PropTypes.ReactNode,
      description: "The content of the modal",
      value: "",
      noEdit: true
    }
  ]}
  initialCode={`
() => (
<Popover.Root>
  <Popover.Trigger>
    <Button>Adjust size</Button>
  </Popover.Trigger>
  <Popover.Content>
    <Text variant="h5">Dimensions</Text>
    <div className="mt-size-2y space-y-2" data-size="sm">
      <div className="grid place-items-center grid-cols-4 gap-2">
        <Text>Width</Text>
        <Input className="w-full col-span-3" defaultValue="100%" />
      </div>
      <div className="grid place-items-center grid-cols-4 gap-2">
        <Text>Height</Text>
        <Input className="w-full col-span-3" defaultValue="100%" />
      </div>
    </div>
  </Popover.Content>
</Popover.Root>
)`}
  scope={{
    Input,
    Button,
    Card,
    Text,
    Popover
  }}
  imports={{
    "@parssa/universal-ui": {
      named: ["Input", "Button", "Card", "Text", "Popover"]
    }
  }}
/>

### Anatomy

<CodeBlock className="my-size-2y">
  {`<Popover>
  <Popover.Trigger> 
    {/* ... */}
  </Popover.Trigger>
  <Popover.Content>
    {/* ... */}
  <Popover.Content>
</Popover>`}
</CodeBlock>

<RadixDisclaimer
  componentName="Popover"
  radixComponentName="Popover"
  radixURL="https://www.radix-ui.com/docs/primitives/components/popover"
/>

export default ({ children }) => <DocsMDXLayout meta={meta}>{children}</DocsMDXLayout>;
